/**
 * TaskPilotAI – Authentication Module
 * File: frontend/js/auth.js
 *
 * Responsibilities:
 *   - Wrap Supabase Auth methods (signUp, signInWithPassword, signOut,
 *     onAuthStateChange, resetPasswordForEmail)
 *   - Expose helper functions consumed by login.html, signup.html, and main.js
 *   - Handle post-auth redirects (e.g., to dashboard after login)
 *   - Store/clear the active session token as needed
 *
 * NOTE: All authentication is delegated to Supabase. Do NOT implement a
 *       custom auth layer or duplicate logic in the FastAPI backend.
 *
 * Dependencies:
 *   - supabase.js (Supabase client instance)
 *
 * Implementation deferred to the authentication development step.
 */
