/**
 * TaskPilotAI – UI Utilities Module
 * File: frontend/js/ui.js
 *
 * Responsibilities:
 *   - Dynamically load and inject HTML component files (navbar, sidebar,
 *     footer, notification toast) into the current page
 *   - Expose helper functions for showing/hiding elements, toggling classes,
 *     rendering skeleton loaders, and managing modal dialogs
 *   - Manage the global notification toast (show, dismiss, auto-dismiss)
 *   - Handle sidebar collapse/expand state and active nav-link highlighting
 *
 * Dependencies: None (pure DOM API)
 *
 * Implementation deferred to the UI development step.
 */
